//Lab9
//Makenna Jones

#include <iostream>
#include <iomanip>
#include <cstdlib>
#include <string>
using namespace std;

int dailyLow[30];
int averageLow;
int highestLow;
int lowestLow;

int main(){
    ifstream instream; 
    string filename = "athens_low_temps.txt"
    instream.open (filename);
    if instream (fail())
        cout << "Error, can't open file" << endl;
        exit(0);
   
    if instream (fail()){
    for (int i = 0, i < 30, i++)    
    instream >> dailyLow(){
        }
    }
    lowest = lowestLow()
    highest= highestLow()
    average = averageLow()
}
